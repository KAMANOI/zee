name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

# Least privilege: the matrix only needs to read the repo and run code.
permissions:
  contents: read

# Concurrency: cancel in-progress runs on the same ref when a new one starts.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: ${{ matrix.os }} / Python ${{ matrix.python }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python: ['3.11', '3.12', '3.13']
    steps:
      # Actions pinned by SHA. Dependabot keeps them current.
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

      - name: Set up Python ${{ matrix.python }}
        uses: actions/setup-python@0a5c61591373683505ea898e09a3ea4f39ef2b9c  # v5.0.0
        with:
          python-version: ${{ matrix.python }}

      - name: Install zee with test extras
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[test]"

      - name: Show installed version
        run: zee --version

      - name: Run pytest
        run: pytest tests/ -q

      - name: Smoke test — capability matrix
        # Locale check: the Windows ja-JP runner is not available on
        # GitHub-hosted Actions; we exercise the parser via the unit
        # tests in test_windows_iface_enumeration.py. The smoke step
        # here only confirms the CLI is wired and prints a matrix
        # the operator could read.
        run: zee capability
        shell: bash

      - name: Smoke test — init-restore-token
        # Confirms the v0.3 restore_token plumbing runs end-to-end on
        # all three OSes. The token file is written to a tmp HOME so
        # the runner's $HOME stays unaffected.
        run: |
          mkdir -p tmp_home
          HOME="$PWD/tmp_home" zee init-restore-token > /dev/null
          test -f "$PWD/tmp_home/.zee/restore_token" || (echo "restore_token not created" && exit 1)
        shell: bash

  # A standalone job that exercises the canary-configured path: same
  # 78-test suite, but with ZEE_CANARY_BASE_URL set so the
  # canary_configured branches in capability / seeder / watcher are
  # also under coverage.
  canary-configured:
    name: canary-configured smoke
    runs-on: ubuntu-latest
    env:
      ZEE_CANARY_BASE_URL: https://canary.test.invalid/r
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

      - name: Set up Python 3.13
        uses: actions/setup-python@0a5c61591373683505ea898e09a3ea4f39ef2b9c  # v5.0.0
        with:
          python-version: '3.13'

      - name: Install zee with test extras
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[test]"

      - name: Run pytest under canary-configured env
        run: pytest tests/ -q

      - name: Verify capability prints "configured"
        # Match the exact "canary base_url : configured" header line
        # so future "pre-configured" / "auto-configured" notes do not
        # produce a false positive.
        run: zee capability | grep -E "^canary base_url\s*:\s*configured"
